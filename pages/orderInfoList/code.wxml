<view class="container">
	<!-- 二维码放大图 -->
	<view class='qrcodeWrap' wx:if="{{codeFlag == 1}}">
		<image class="qrcode" src='{{codeurl}}'></image>
	</view>
	<!-- 条形码放大图 -->
	<view class='barcodeWrap' wx:elif="{{codeFlag == 0}}">
		<image class="barcode" src='{{codeurl}}'></image>
	</view>
</view>